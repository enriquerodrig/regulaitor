# v0.1.17.1 — No-Answer residual fix (TWO-part + 5-bucket extension) — Design Spec

> Decimal sub-milestone following v0.1.17 (precedent: H0.1, H15.1, H15.2,
> v0.1.17). Maximalist plan microhito **10b/12**. $0 throughout. Intervention
> derived from the v0.1.17 cache-mining diagnostic verdict.

**Status:** Draft — 2026-05-22 — branch `feat/v0.1.17.1-no-answer-fix` from main @ `b5a98a3`
**Deciders:** Project owner.
**Companion ADRs:** 0016 (H15 Auditor calibration — REGULAITOR_ANALYST_PROMPT_VERSION env seam this extends), 0020 (v0.1.15 chat gap-analysis — Analyst prompt v1.3 this builds on), 0021 (v0.1.16 dual-layer thresholds — same v0.1.20 acceptance ritual will measure v1.4), 0022 (v0.1.17 no-Answer diagnostic — the evidence that drove this intervention).

---

## 1. Context

The v0.1.17 cache-mining diagnostic (`docs/no_answer_residual_diagnosis.md`,
ADR-0022) classified **12 no_answer cases** across 3 reports (H10 baseline,
H15 v1.2 candidate, H15 v1.2 holdout) into the 4-bucket taxonomy:

- refusal: 0/12 (0%)
- analyst_raise: 0/12 (0%)
- transport_error: 2/12 (17%)
- **other: 10/12 (83%)** — dominant

Per ADR-0022 D1's conditional intervention rules: other-dominant → v0.1.17.1
expand REFUSAL_PHRASES seed + re-run.

**But the diagnostic-first design paid off**: inspecting the 10 `other` cases
per-case (beyond mechanical classifier output) reveals a **5th mechanism the
4-bucket taxonomy didn't anticipate**:

- **8 of 10 `other` cases are prose-without-findings**: the Analyst emits
  substantive prose answers in the `text` field (real RGPD / AI Act / DORA
  content) but fails to structure them as `Finding` objects with citations.
  Examples: chat-003 "El AI Act impone a los proveedores…", chat-019 "Según
  el RGPD, el tratamiento de categorías especiales…", dora-001 "El Reglamento
  DORA impone a las entidades financieras…".
- **2 of 10 `other` cases are redteam-block refusals with phrasings outside
  the 22-entry seed list**: chat-014 "Esta solicitud no puede ser atendida…",
  chat-015 "Esta consulta no puede ser atendida…". The Analyst correctly
  refused, but the diagnostic missed the refusal because the seed didn't
  cover the "atendida" phrasing.

A fix-first prompt v1.4 (skipping the v0.1.17 diagnostic) would have addressed
only the refusal-phrasing aspect (which is 2 of 12 cases = 17%, secondary),
not the dominant prose-without-findings pattern (8 of 12 = 67%). The
diagnostic exposed the 5th mechanism, which redirects the intervention.

The v0.1.17.1 intervention is therefore TWO-part with a third documentation
component:

- **(a)** Expand REFUSAL_PHRASES seed to catch the "atendida" patterns
  (reclassifies the 2 redteam-block cases from `other` → `refusal`).
- **(b)** Introduce Analyst prompt **v1.4** with a new Hard Rule 9 forcing
  Finding emission when `text` contains substantive claims (addresses the 8
  prose-without-findings cases at the source).
- **(c)** Extend the classifier taxonomy from 4 → 5 buckets, adding
  `prose_without_findings` as an explicit bucket, so future runs directly
  count the 5th mechanism instead of inferring from `other`.

Backend (H1-H5/H7), Auditor + citation-validator (§6 invariant), eval pipeline
(judge/cache/harness/metrics/schemas/report), prior Analyst prompts
(v1.0/v1.1/v1.2/v1.3), and the gold set stay BYTE-UNCHANGED. v0.1.17.1 is
prompt-file + script + tests + docs only.

---

## 2. Decisions

### D1 — Approach = intervention + observability (no measurement)

Ship the intervention (v1.4 prompt + seed expansion) AND the diagnostic
observability extension (5th bucket). DO NOT measure v1.4 effectiveness with a
paid LLM run in v0.1.17.1. Empirical A/B v1.0 vs v1.4 on the 30-case cohort
goes into the **v0.1.20 paid bundle** (alongside all maximalist-plan
optimizations, per the bundled-validation discipline established v0.1.8).

Rationale: $0 budget remaining; v0.1.20 bundled measurement amortizes paid run
across multiple capabilities (v1.4 + v0.1.10/v0.1.11/v0.1.12 retrieval
levers + v0.1.13/v0.1.15 gold extensions + v0.1.14 segmenter); v1.4
production-default flip happens in the v0.1.20 acceptance ritual against the
v0.1.20-bar (ADR-0021) — NOT in v0.1.17.1.

### D2 — REFUSAL_PHRASES expansion = 3 phrases, evidence-driven

Add to `REFUSAL_PHRASES_ES` in `scripts/diagnose_no_answer.py`:

1. `"esta solicitud no puede ser atendida"` — verbatim from chat-014.
2. `"esta consulta no puede ser atendida"` — verbatim from chat-015.
3. `"no se puede atender"` — catch-all for "atender" pattern (covers nearby
   phrasings like "esta petición no se puede atender", "tu pregunta no se
   puede atender" that the v1.0/v1.1/v1.2/v1.3 model variants might emit).

Counts: 16 → 19 ES, 6 → 6 EN, total 22 → **25**. Length-sort behavior in
`_find_refusal_phrase` (added v0.1.17 T2) preserved — longest-phrase-first
ensures "esta solicitud no puede ser atendida" matches before
"no se puede atender" for cleaner `matched_phrase` reporting (no outcome
change vs unsorted).

NOT speculative catch-all expansion: only the evidence-driven 3 phrases. If
v0.1.17.1's re-run still shows residual `other` cases with new refusal
phrasings, those carry to a future v0.1.17.2 (or get absorbed into v0.1.20
acceptance narrative). The 25-entry seed is an evidence-driven update, not
prophylaxis.

### D3 — Analyst prompt v1.4 = Hard Rule 9 + Output contract amendment

NEW file: `src/regulaitor/agents/prompts/analyst/system.v1.4.md`. Built on
top of v1.3 (preserves gap-analysis chat mode from v0.1.15). Production
default stays **v1.0** (boundary contract carried verbatim: env-unset =
v0.1.17 byte-identical); v1.4 opt-in via
`REGULAITOR_ANALYST_PROMPT_VERSION=v1.4` for v0.1.20 paid bundle measurement.

**Hard Rule 9 (NEW, inserted after Rule 8, before Output format header):**

```markdown
9. **A substantive answer in `text` without `findings` is INVALID.** If your
   `text` field contains any substantive claim about what the law requires,
   permits, prohibits, or how a corpus article applies — even one sentence
   beyond pure acknowledgement — you MUST emit at least one `Finding` with
   ≥1 citation grounding that claim. Emitting prose into `text` while
   leaving `findings: []` violates §6 ("no citation, no answer") and the
   Auditor will auto-reject the answer. Before returning, perform a
   self-check: "Did I make any normative or factual claim about the corpus
   in `text`? If yes, is each such claim represented by ≥1 Finding with a
   literal citation?" If a claim is not represented, either (i) add a
   Finding for it, or (ii) remove the claim from `text` and emit a
   structured refusal instead (Output contract Rule 2). There is no third
   option.
```

**Output contract amendment** (sharpens existing line 85-86 of v1.3):

Before: `- If the context supports an answer: emit \`findings\` with >=1 finding, each with its minimal supporting citation set (see Hard rule 6).`

After: `- If the context supports an answer: emit \`findings\` with >=1 finding, each with its minimal supporting citation set (see Hard rule 6). Per Hard rule 9, every substantive claim in \`text\` must map to ≥1 Finding — empty \`findings\` with non-empty substantive \`text\` is INVALID.`

**Design notes:**

- **"Substantive" not "any"** — `text` like "Has declarado: X, Y"
  (gap-analysis acknowledgement) or "El AI Act no contiene disposiciones
  aplicables a tu consulta" (refusal) is NOT a normative claim about the
  corpus — those legitimately stay without Findings.
- **Self-check instruction** — forces the model to reason about its own
  output before tool emission, mirroring v1.2 Hard Rule 6's
  single-most-directly-supporting-article self-check that worked.
- **No JUNK-Finding risk** — self-check explicitly offers "remove the claim
  from `text` and emit a refusal" as alternative; gives the model an out
  instead of fabricating empty Findings to satisfy a quota.
- **No example added** — the existing 3 examples (Q&A + 2 gap-analysis)
  already model correct behavior. Adding a "what NOT to do" example risks
  the model copying the malformed pattern.
- **Hard rules 1-8 + Output format + Output contract — gap-analysis branch +
  Examples 1-3 byte-identical to v1.3** (regression-zero on gap-analysis chat
  mode + Q&A path).

### D4 — Classifier 5th bucket = `prose_without_findings` with 100-char heuristic

Extend `classify_no_answer_case` in `scripts/diagnose_no_answer.py`:

```
3. If cache entry found, inspect actual_answer:
   - Empty string OR "(backend error)" sentinel → "transport_error"
   - Contains any REFUSAL_PHRASES phrase (case-insensitive substring) → "refusal"
   - Non-empty + no refusal phrase + len(actual_answer.strip()) > 100 → "prose_without_findings"   # NEW
   - Non-empty + no refusal phrase + len(actual_answer.strip()) ≤ 100 → "other"
```

**Why 100 chars:**

- All 8 observed prose-without-findings cases from v0.1.17 diagnostic are
  ≥ 200 chars (substantive paragraphs starting with "El AI Act impone…",
  "Según el RGPD…", "El Reglamento DORA impone…").
- A 100-char ceiling on `other` keeps the bucket for genuine edge cases (very
  short non-refusal outputs that don't fit any clean category — e.g.
  structured-but-incomplete fragments, single-sentence acknowledgements that
  don't trigger refusal phrases).
- **Conservative**: better to under-classify (some short prose stays `other`)
  than over-classify (chopping legitimate-but-short refusals as prose). The
  `other` bucket already documents itself as needing manual review, so
  residual short-prose cases stay visible.

**Honest caveat in produced markdown** (per §22.22):

> "The 100-char threshold is a heuristic motivated by the v0.1.17 observation
> that all 8 prose-without-findings cases exceeded 200 chars. Future
> diagnostic runs may surface short substantive prose cases that the
> threshold misses — these stay in `other` and remain visible for manual
> review."

**`intervention_recommendation` 5th branch**:

```python
if counts["prose_without_findings"] / total > 0.50:
    return (
        "prose_without_findings-dominant — Analyst emits substantive prose "
        "in `text` without structured Findings. Ship Analyst prompt v1.4 "
        "(force-Finding-emission Hard Rule) — this milestone (v0.1.17.1) "
        "implements exactly that intervention."
    )
```

**`render_diagnosis_markdown` 5-bucket rendering**: aggregate counts section
gains a 5th row; per-case table classification column shows the 5-way label;
trajectory analysis section compares 5-bucket distribution across reports.

### D5 — Diagnostic re-run = committed artifact in v0.1.17.1 closure

After (a)+(b)+(c) are in place, controller-runs
`python -m scripts.diagnose_no_answer` against the same cache snapshot used
in v0.1.17 (regenerable via `make eval-from-cache` if needed). Updated
`docs/no_answer_residual_diagnosis.md` committed as v0.1.17.1 closure
artifact. Expected reclassification (honest, not promised):

- `refusal`: 0 → ≥2 (chat-014/015 reclassify from `other`).
- `prose_without_findings`: 0 → ≥6 (the 8 substantive-prose cases minus the
  2 redteam-block phrasings now in refusal; conservative because the 100-char
  threshold may classify some short substantive cases back to `other`).
- `other`: 10 → ≤2 (residual ambiguous cases requiring manual review).
- `transport_error`: 2 → 2 (unchanged, no change in cache).
- `analyst_raise`: 0 → 0 (unchanged).

The re-run is the diagnostic instrument validating that (a) and (c) work
as designed. The v1.4 prompt's effectiveness is NOT measured here (deferred
to v0.1.20 per D1).

---

## 3. Architecture

### 3.1 Files touched

| File | Change | Lines (approx) | Why |
|------|--------|----------------|-----|
| `src/regulaitor/agents/prompts/analyst/system.v1.4.md` | CREATE | ~220 (v1.3 + Rule 9 + amendment) | The intervention prompt |
| `scripts/diagnose_no_answer.py` | MODIFY | +25 (3 REFUSAL_PHRASES + 5th bucket logic + recommendation + render) | Seed expansion + 5-bucket classifier |
| `tests/unit/test_analyst_v1_4_loads.py` | CREATE | ~120 (8 tests) | Pin v1.4 prompt invariants |
| `tests/unit/scripts/test_diagnose_no_answer.py` | MODIFY | +60 (4 new tests + update pinned counts test) | Pin 5th bucket + 25-count + 3 new phrases |
| `docs/no_answer_residual_diagnosis.md` | OVERWRITE (by re-run) | ~200 (regenerated 5-bucket version) | Updated closure artifact |
| `docs/adr/0023-no-answer-fix.md` | CREATE | ~110 (mirror ADR-0022 structure) | Decision record |
| `docs/technical_decisions_log.md` | APPEND §v0.1.17.1 | ~50 | Living log |
| `docs/evidence_matrix.md` | EDIT 3 spots | ~3 | ADR count 22→23, line refresh, tag row |
| `CLAUDE.md` | EDIT §16.3 + §27 | ~6 | v0.1.17.1 entry + Hito siguiente flip |

ADR count: 22 → **23**. Test count: 867 → ~875 (+8 v1_4_loads + 4 new in
test_diagnose_no_answer; the 1 existing pinned-count test is updated, not
added).

### 3.2 Tests

**$0 unit tests only.** No paid LLM run.

**NEW `tests/unit/test_analyst_v1_4_loads.py` (~8 tests):**

1. `test_v1_4_prompt_loads` — file exists at expected path, parses frontmatter
   + body, version = "1.4".
2. `test_v1_4_preserves_hard_rules_1_through_8_verbatim` — regression anchor
   on gap-analysis chat mode.
3. `test_v1_4_adds_hard_rule_9` — Hard Rule 9 present with anchors
   ("substantive answer", "INVALID", "no citation, no answer", "self-check").
4. `test_v1_4_output_contract_amended` — line 85-86 amendment present
   ("every substantive claim in `text` must map to ≥1 Finding").
5. `test_v1_4_preserves_output_format_section_verbatim` — Output format
   byte-identical to v1.3 (no schema change).
6. `test_v1_4_preserves_examples_1_2_3_verbatim` — three examples
   byte-identical to v1.3 (regression anchor on demonstrated correct
   behavior).
7. `test_v1_4_preserves_gap_analysis_branch_verbatim` — Output contract —
   gap-analysis branch section byte-identical to v1.3 (regression anchor on
   v0.1.15 work).
8. `test_v1_4_changelog_documents_v0_1_17_1` — frontmatter changelog has a
   2026-05-22 entry referencing v0.1.17.1 + Hard Rule 9 +
   force-Finding-emission.

**MODIFY `tests/unit/scripts/test_diagnose_no_answer.py` (+4 new, 1 updated):**

9. `test_refusal_phrases_seed_list_pinned` — UPDATE counts (16→19, 22→25)
   + ADD 3 assertions for new ES phrases.
10. `test_classify_prose_without_findings_when_long_substantive` (NEW) —
    cache entry present + non-empty + no refusal phrase + actual_answer
    250 chars → returns `"prose_without_findings"`. Uses verbatim sample from
    `docs/no_answer_residual_diagnosis.md` per-case table.
11. `test_classify_other_when_short_non_refusal` (NEW) — cache entry present
    + non-empty + no refusal phrase + actual_answer 50 chars → returns
    `"other"` (boundary test for 100-char threshold).
12. `test_classify_refusal_with_new_atendida_phrases` (NEW) — cache entry
    present + "esta solicitud no puede ser atendida" in actual_answer →
    returns `"refusal"` (regression anchor: chat-014-style cases now
    classify correctly).
13. `test_recommend_intervention_handles_prose_without_findings_dominant`
    (NEW) — counts `{prose_without_findings: 8, refusal: 2, ...}` → string
    mentions "prose_without_findings-dominant" + "v1.4" +
    "force-Finding-emission".

**No empirical re-run test**: the controller-level T4 re-run of
`python -m scripts.diagnose_no_answer` updates
`docs/no_answer_residual_diagnosis.md` and is committed as the closure
artifact. Its output is inspected (not asserted) — pinning specific counts
in a test would couple test stability to cache contents.

### 3.3 Backend / production behavior

ZERO change. The v1.4 prompt is opt-in via env (`REGULAITOR_ANALYST_PROMPT_VERSION=v1.4`); env-unset still loads v1.0 (boundary contract verbatim from v0.1.15+). The Analyst code (`src/regulaitor/agents/analyst.py`) is byte-unchanged — the env-overridable seam exists since H15 (ADR-0016), v0.1.17.1 just adds v1.4 to the version chain that the existing seam already supports.

Backend H1-H5/H7 + Auditor + citation-validator + eval pipeline + Pydantic schemas + DTOs + gold set ALL byte-unchanged (verified by 5 git-diff HARD checks at T5).

---

## 4. Done-when (closure criteria)

All controller-verified at T5 before squash-merge:

1. **Gate authoritative**: `uv run pytest -m "not slow"` → ≥875 passed / 0 failed / 1 skipped (carry) / ≥90% coverage exit 0.
2. **mypy strict**: `uv run mypy src` → Success, 71 files, exit 0 (UNCHANGED — v0.1.17.1 adds no `.py` file under `src/`).
3. **Redteam smoke carry**: `make redteam-smoke` → block_rate 0.92 (= v0.1.14/v0.1.15/v0.1.16/v0.1.17 carry). `git checkout -- redteam/reports/latest.md` after run.
4. **§6 invariant HARD git-diff (must be empty)** — `git diff main...HEAD -- src/regulaitor/agents/auditor.py src/regulaitor/citation/validator.py` returns nothing.
5. **Backend H1-H5/H7 HARD git-diff (`src/` shows ONLY the new prompt)** — `git diff main...HEAD --stat -- src/` shows ONLY `src/regulaitor/agents/prompts/analyst/system.v1.4.md`. NO touch to any `.py` file.
6. **Eval pipeline HARD git-diff (must be empty)** — `git diff main...HEAD -- evals/judge.py evals/cache.py evals/harness.py evals/metrics.py evals/schemas.py evals/report.py` returns nothing.
7. **Prior Analyst prompts HARD git-diff (must be empty)** — `git diff main...HEAD -- src/regulaitor/agents/prompts/analyst/system.v1.{0,1,2,3}.md` returns nothing.
8. **Gold set byte-unchanged** — `git diff main...HEAD -- evals/gold_set.jsonl` returns nothing.
9. **Re-run diagnostic verdict** — `docs/no_answer_residual_diagnosis.md` updated with new aggregate counts showing reclassification per D5 (honest about per-case table, not promised counts — specific numbers depend on actual reclassification).
10. **Production default unchanged** — `REGULAITOR_ANALYST_PROMPT_VERSION` env-unset still loads v1.0 (boundary contract carried verbatim).

---

## 5. Out of scope (explicit non-goals)

- **Paid LLM run** — empirical A/B v1.0 vs v1.4 deferred to v0.1.20 paid bundle.
- **Promotion of v1.4 to production default** — production default stays v1.0; flip to v1.4 happens at v0.1.20 acceptance ritual ONLY if measured against v0.1.20-bar (ADR-0021) and decided in the v0.1.20 decisions_log narrative.
- **Backend touch** — Analyst code, router, Auditor, citation-validator, eval pipeline, Pydantic schemas, DTOs, gold set ALL byte-unchanged.
- **New gold cases** — 64 chat + 10 doc cases stay byte-unchanged (regression-zero on gold corpus).
- **New ADR for the Analyst env seam** — ADR-0016 (H15) already established the env-overridable pattern; v0.1.17.1 just adds v1.4 to the existing version chain. The new ADR-0023 documents the NO-ANSWER FIX intervention WHAT/WHY.
- **`analyst_raise` vs `transport_error` disambiguation when cache absent** — v0.1.17 documented this as an accepted ambiguity; v0.1.17.1 inherits the caveat unchanged.
- **Expansion beyond the 3 new REFUSAL_PHRASES** — evidence-driven only; future v0.1.17.2 (or v0.1.20 acceptance narrative) handles further drift.
- **6th bucket** — if v0.1.20 paid run surfaces a new mechanism, future taxonomy extension. 5-bucket is the v0.1.17.1 closure shape.
- **Doc-mode v1.4** — Hard Rule 9 applies to chat-mode Analyst only. Doc-mode (H5 pipeline) uses different prompt lineage; no touch in v0.1.17.1.
- **Streamlit / API surface change** — the prompt-version env is read at Analyst init, surfaces nowhere new in UI / API.
- **Measurement of v1.4 against the v0.1.15 gap-analysis 10-case gold subset** — gap-analysis cases already emit Findings per Output contract gap-analysis branch; verification stays at prompt-fidelity test level (test 7 above pins gap-analysis branch byte-identical).

---

## 6. Alternatives considered

- **Fix-first prompt v1.4 (skip v0.1.17 diagnostic entirely)** — already rejected in ADR-0022 D1 alternatives. The diagnostic-first design paid off here: v0.1.17 exposed the prose-without-findings 5th mechanism, which redirects v1.4's wording from "expand structured-refusal contract" (the speculative target) to "force Finding emission on substantive prose" (the evidence-driven target).
- **Speculative seed expansion (10-20 new REFUSAL_PHRASES)** — rejected. Only the 3 evidence-driven phrases are added. Prophylactic expansion risks false-positives (legitimate prose accidentally containing common phrases). Re-run after (a)+(c) shows whether expansion is sufficient.
- **6-bucket taxonomy** — premature. 5-bucket addresses the observed 5th mechanism; a 6th bucket waits for evidence of a 6th mechanism in v0.1.20 paid run.
- **Ship (a) and (c) only, defer (b) v1.4 prompt to v0.1.17.2** — rejected. The v1.4 prompt is the intervention closest to the dominant pattern; deferring it would leave v0.1.17.1 as a pure observability update with no actual fix. The bundling of (a)+(b)+(c) in one milestone is appropriate because they all address the same evidence and don't require paid measurement to ship (only v0.1.20 measurement to validate effectiveness).
- **Promote v1.4 to production default in v0.1.17.1** — rejected. Production-default flip is governed by v0.1.20-bar measurement (ADR-0021); flipping without measurement would violate §22.22 (no non-measured claims).
- **Stricter Hard Rule 9 wording ("MUST emit ≥1 Finding for any non-empty `text`")** — rejected. Too strict: would force JUNK Findings on legitimate acknowledgements (gap-analysis "Has declarado…") and refusals. The "substantive claim" framing + self-check + explicit "remove the claim" out is the right calibration.
- **Softer Hard Rule 9 wording ("should emit Findings when appropriate")** — rejected. Too soft: empirical evidence from v0.1.17 shows the current "If the context supports an answer: emit findings" permission framing already fails. The intervention needs absolute language with self-check enforcement.

---

## 7. Consequences

**Positive:**

- **Evidence-driven intervention**: the v1.4 prompt + seed expansion target
  the actual dominant patterns from v0.1.17 (not speculative guesses).
- **§6 invariant strengthened**: Hard Rule 9 makes the "no citation, no
  answer" invariant explicit at the prompt level, not just enforced
  downstream by the Auditor.
- **Boundary contract preserved**: production default stays v1.0; v1.4 is
  opt-in via env. Zero risk to current production behavior.
- **5-bucket taxonomy reusable**: future diagnostic re-runs (v0.1.20, post-TFM)
  directly count the 5th mechanism instead of inferring from `other`.
- **$0 milestone**: cache-mining re-run + unit tests; no paid LLM.

**Negative / accepted (per §22.22):**

- **No empirical validation in v0.1.17.1**: the v1.4 prompt's effectiveness
  at reducing prose-without-findings is NOT measured here. v0.1.17.1 ships
  the capability; v0.1.20 paid bundle measures it. Honest framing carried.
- **100-char heuristic threshold**: short substantive prose cases (rare but
  possible) stay in `other` and require manual review. Caveat documented in
  the produced markdown.
- **3-phrase seed expansion is non-exhaustive**: if the model emits new
  refusal phrasings not covered, they classify as `prose_without_findings`
  (if >100 chars) or `other` (if ≤100 chars). Future v0.1.17.2 or v0.1.20
  acceptance narrative handles further drift.
- **No JUNK-Finding empirical risk assessment**: Hard Rule 9 explicitly
  guards against JUNK Findings via self-check + "remove the claim" out, but
  the actual model behavior under v1.4 is unmeasured until v0.1.20.
- **Cross-prompt regression risk at v1.4**: Hard Rule 9 + Output contract
  amendment interact with v1.3's gap-analysis Hard Rule 8 in ways unmeasured
  here. The "preserves gap-analysis branch byte-identical" test (test 7)
  pins the prompt structure, but model behavior under v1.4 on gap-analysis
  cases is not validated until v0.1.20.

---

## 8. References

- ADR-0022 (v0.1.17 no-Answer residual diagnostic — the evidence driving this).
- ADR-0021 (v0.1.16 dual-layer thresholds — v0.1.20-bar measurement venue).
- ADR-0020 (v0.1.15 gap-analysis chat mode — v1.3 base preserved by v1.4).
- ADR-0016 (H15 Auditor calibration — REGULAITOR_ANALYST_PROMPT_VERSION env seam).
- `docs/no_answer_residual_diagnosis.md` (v0.1.17 output — per-case evidence).
- `src/regulaitor/agents/prompts/analyst/system.v1.3.md` (v1.4 base).
- `scripts/diagnose_no_answer.py` (the classifier extended in D2+D4).
- `tests/unit/scripts/test_diagnose_no_answer.py` (the test file modified).
- `feedback_cost_estimation_discipline.md` (carried — no paid run without bundle).
- v0.1.20 paid bundle (future) — empirical A/B v1.0 vs v1.4.

---

## 9. Closure ceremony

Same as v0.1.17 / v0.1.16 / v0.1.15:

- Tag: `v0.1.17.1-no-answer-fix` (decimal sub-milestone).
- Squash-merge `feat/v0.1.17.1-no-answer-fix` → main; annotated tag on squash commit.
- Post-merge `<squash-sha>` populate (5 spots: ADR-0023 Status, decisions_log §v0.1.17.1 header, evidence_matrix tag row, CLAUDE.md §16.3 v0.1.17.1 entry, CLAUDE.md §27 Hitos cerrados v0.1.17.1 bullet).
- Memory roll-forward: `v0.1.17_closed_v0.1.17.1_starting.md` → `v0.1.17.1_closed_v0.1.18_starting.md` + MEMORY.md index update.
- Branch delete via `git branch -D` (squash workflow).
- NO push to origin until user authorizes separately.
- Maximalist plan microhito **10b/12** done; next = **v0.1.18** (citation granularity confound).
