# v0.1.18 — Citation granularity confound (eval-instrument fix) — Design Spec

> Decimal sub-milestone following v0.1.17.1 (precedent: H0.1, H15.1, H15.2,
> v0.1.17, v0.1.17.1). Maximalist plan microhito **12/12** (final $0
> microhito before v0.1.19 + v0.1.20). $0 throughout. Eval-instrument fix
> derived from the H15.1 §22.22 design-defect disclosure.

**Status:** Draft — 2026-05-22 — branch `feat/v0.1.18-citation-granularity` from main @ `1325239`
**Deciders:** Project owner.
**Companion ADRs:** 0010 (H8 evaluation harness — the canonical citation_precision/recall metric instrument this fixes; D7 cache-stores-judge-only is the substrate that enables $0 re-rendering), 0015 (H14 cross-corpus expansion — introduced the article-level expected_articles annotation style that exposed the H8 apartado-level baseline assumption), 0017 (H15.1 retriever optimization — the §22.22 design-defect disclosure on instrument invariance that flagged this exact confound), 0021 (v0.1.16 dual-layer thresholds + v0.1.20-bar — the measurement venue that requires this instrument fix for a fair denominator), 0023 (v0.1.17.1 no-Answer fix — the immediately-preceding eval-side milestone, established the boundary-contract preservation pattern this carries).

---

## 1. Context

The H15.1 §22.22 design-defect disclosure (ADR-0017, `docs/retriever_optimization.md`) flagged that v1.2's holdout citation=0.00 is dominated by the eval instrument's exact-match contract — NOT a measurement of v1.2's (or any future Analyst prompt's) actual citation quality.

The empirical evidence (collected during this brainstorming):

**Gold set granularity distribution (64 chat cases):**

| Bucket | Cases | Article-level expected | Apartado-level expected |
|--------|-------|------------------------|-------------------------|
| chat (H8) | 30 | 1 (chat-028, intentional — RGPD art 44 general principle) | 38 |
| nis2/dora/xcorpus (H14) | 14 | 20 | 0 |
| industry (v0.1.13/v0.1.15) | 20 | 70 | 0 |
| **TOTAL** | **64** | **91** | **38** |

**Holdout report sample (`evals/reports/h15/holdout-v1.2-chat.md`):**

```
- emitted=['2.2', '3.1', '3.2', '3.3']  expected=['2', '3']    precision=0.00  recall=0.00
- emitted=['20.1', '20.2', '21.1', '33.4']  expected=['21']    precision=0.00  recall=0.00
- emitted=['23.4']  expected=['23']                              precision=0.00  recall=0.00
- emitted=['20.1', '20.2']  expected=['20']                      precision=0.00  recall=0.00
- emitted=['32.4', '32.5', '34.1', '34.4', '34.6']  expected=['32', '33', '34']  precision=0.00  recall=0.00
```

Every 0.00/0.00 line is a granularity mismatch where the Analyst correctly cited apartados within the expected article. Under hierarchical containment match, the first row alone is precision=1.00 + recall=1.00 (all 4 emitted are within the 2 expected articles; both expected articles are covered).

**H8 baseline sample (`evals/reports/latest.md`):**

```
- emitted=['6.2', '6.3', '6.4', '80.1']  expected=['6.2', '6.3']  precision=0.50  recall=1.00  (works — exact apartado)
- emitted=['26.11', '26.5', '26.7', '26.8', '27.1', '27.3']  expected=['13.1', '13.2']  precision=0.00  recall=0.00  (legitimate miss — wrong articles)
```

H8 apartado-vs-apartado comparisons already work; H8 cases with genuinely-wrong-article citations stay legitimate misses (the new rule is asymmetric — apartado-expected requires apartado-specific match).

The fix needs to live in `evals/metrics.py::compute_citation_metrics` (where the simple set-intersection currently happens). The §6 invariant (citation/validator.py) is NOT touched — production-side citation VALIDATION (does the citation exist in the corpus? does the text match literally?) stays byte-unchanged. v0.1.18 fixes only the post-hoc EVAL precision/recall metric that scores how well the Analyst's correct-by-validator citations align with the gold annotator's expected set.

v0.1.18 ships the instrument fix so v0.1.20 paid bundle measurement has a fair denominator across the heterogeneous gold set (29 H8 apartado + 1 H8 article + 34 H14/industry article).

---

## 2. Decisions

### D1 — Match semantics = hierarchical containment with directional asymmetry

Replace `compute_citation_metrics`'s set-intersection with a per-pair match rule `_citation_matches(emitted: str, expected: str) -> bool`:

| expected | emitted | match | rationale |
|----------|---------|-------|-----------|
| `"X"`    | `"X"`   | True  | exact article-level |
| `"X"`    | `"X.Y"` | True  | article-expected matches any apartado of X (the question doesn't care which sub-clause) |
| `"X.Y"`  | `"X.Y"` | True  | exact apartado-level |
| `"X.Y"`  | `"X.Z"` | False | different apartados = different obligations |
| `"X.Y"`  | `"X"`   | False | apartado-expected requires apartado-specific match; article-only emitted is too coarse |
| `"X"`    | `"W.Y"` | False | different article (W ≠ X) |
| `"X.Y"`  | `"W.Z"` | False | different article (W ≠ X) |

The asymmetry is deliberate: an article-only expected is a coarser target that ANY of its apartados satisfies (chat-028 RGPD art 44 general principle, every H14/industry case). An apartado-only expected is a finer target requiring the right sub-clause (chat-001 art 6.1 is the high-risk definition vs art 6.2 is the listing — different obligations).

Rejected alternatives (per Q1 brainstorming):

- **Normalize both to article granularity** — rejected. Loses H8's apartado-discrimination signal (29/30 H8 cases legitimately want apartado precision; chat-001 expected=['6.1','6.2'] vs emitted=['6.3','6.7'] should NOT match).
- **Hierarchical with partial credit** — rejected as YAGNI. v0.1.20 measurement goal is a fair denominator, not finer-grained signal. Partial-credit semantics complicate aggregation (mean-of-credits ≠ mean-of-Booleans).

### D2 — Gold set byte-unchanged (pure instrument fix)

No gold re-annotation in v0.1.18. The 64 chat + 10 doc cases stay byte-unchanged. The instrument now handles BOTH legitimate granularities via D1.

chat-028 (the H8 article-level outlier) is intentional — RGPD art 44 is the general principle for international transfers, no specific apartado is the right citation. Confirmed by reading the case during brainstorming. Not annotator drift.

Boundary contract carried verbatim from v0.1.17.1: `evals/gold_set.jsonl` HARD git-diff invariant remains empty at pre-closure gate.

Rejected alternatives (per Q2 brainstorming):

- **Light audit (chat-028 + 3-5 H14 spot-checks)** — rejected. chat-028 already verified intentional; H14 cases are recent and consistent (100% article-level annotation, no apartado-level outliers observed). No evidence of drift to motivate the audit.
- **Full granularity audit (all 64 cases)** — rejected. High-probability YAGNI (most-likely outcome: 0 changes after expensive audit).

### D3 — Re-render historical reports from cache at $0 (retrospective consistency)

Apply the new instrument to existing historical reports by re-running the report renderer against the existing 381-entry judge cache (`evals/cache/`). The metric values recompute at $0 — pure-Python recomputation over cached LLM outputs; no new LLM call needed.

Files re-rendered (committed as evidence):

- `evals/reports/latest.md` (H10 30-case baseline)
- `evals/reports/h15/candidate-v1.2.md`
- `evals/reports/h15/holdout-v1.2-chat.md` (the dramatic one — citation jumps from 0.00 to actual value)
- Any other report under `evals/reports/h15-1/` or `evals/reports/h15-2/` (controller globs to enumerate at T5)

The v0.1.20 baseline comparison then has consistent denominators across all historical anchors. Clean retrospective consistency.

**Mechanism (decided at T0 of implementation):** verify existing `make eval-from-cache` works for re-rendering historical reports. If yes → use it (no new code). If it requires modification or doesn't cover historical paths → ship a one-shot `scripts/rerender_reports.py` (~50 lines, $0).

Rejected alternative (per Q3 brainstorming):

- **Freeze historical + apply only to v0.1.20+ runs** — rejected. Introduces a permanent §22.22 caveat (pre-v0.1.18 numbers use old instrument; direct comparison invalid). The TFM memoria would have to carry that caveat throughout. Retrospective consistency at $0 is cheap insurance against narrative friction.

### D4 — `_format_articulo` UNCHANGED, signature compatibility preserved

`_format_articulo(c: Citation) -> str` still produces `"X.Y"` when apartado present, `"X"` when absent. The new match rule operates on its output. `compute_citation_metrics` signature is unchanged (`emitted: list[str]`, `expected: list[str]`); only its INTERNAL logic changes.

`CitationMetrics` dataclass (the return type) is unchanged: `emitted`, `expected`, `precision`, `recall` fields, all the same types. Callers (harness, report renderer, aggregation) need NO modification.

### D5 — ADR-0024 documents the instrument change + retrospective re-rendering

NEW `docs/adr/0024-citation-granularity.md` (~110 lines, mirrors ADR-0023 structure). Companion ADRs: 0010 + 0015 + 0017 + 0021 + 0023. Documents:

- The empirical evidence (39 apartado + 91 article expected values; 100% of holdout 0.00 lines = granularity mismatch).
- D1-D4 decisions + 3 rejected alternatives.
- The §22.22 carry-forward: H15.1's design-defect disclosure is now resolved.
- The §6 invariant interpretation: production-side citation VALIDATION (citation/validator.py) is byte-unchanged; v0.1.18 fixes only the post-hoc EVAL metric. This distinction matters for TFM defense narrative.

---

## 3. Architecture

### 3.1 Files touched

| File | Change | Lines (approx) | Why |
|------|--------|----------------|-----|
| `evals/metrics.py` | MODIFY | +30 / -10 | New `_citation_matches` helper + rewritten `compute_citation_metrics` |
| `tests/unit/evals/test_metrics.py` (or `tests/unit/test_metrics.py` — implementer locates) | MODIFY (append section) | +150 | 12 new tests pinning the 7-row truth table + 5 aggregate scenarios; any pre-existing `compute_citation_metrics` tests adjusted 1-for-1 if they pin the old set-intersection semantics |
| `evals/reports/latest.md` | OVERWRITE (re-render) | regenerated | H10 30-case baseline with new instrument |
| `evals/reports/h15/candidate-v1.2.md` | OVERWRITE (re-render) | regenerated | H15 v1.2 candidate with new instrument |
| `evals/reports/h15/holdout-v1.2-chat.md` | OVERWRITE (re-render) | regenerated | H15 v1.2 holdout — citation jumps from 0.00 to actual value |
| (any `evals/reports/h15-1/` or `evals/reports/h15-2/` reports) | OVERWRITE (re-render) | regenerated | controller globs to enumerate at T5 |
| `scripts/rerender_reports.py` (NEW, conditional on T0 check) | CREATE | ~50 | One-shot re-render driver; only if `make eval-from-cache` doesn't cover historical paths |
| `docs/adr/0024-citation-granularity.md` | CREATE | ~110 | ADR-0024 |
| `docs/technical_decisions_log.md` | APPEND §v0.1.18 | ~50 | WHAT/WHY/HOW/IMPACT closure narrative |
| `docs/evidence_matrix.md` | EDIT 3 spots | ~3 | ADR count 23→24, line refresh, v0.1.18 tag-table row |
| `CLAUDE.md` | EDIT §16.3 + §27 | ~6 | v0.1.18 entry + Hito siguiente flip → v0.1.19 |

**ADR count:** 23 → **24**. **Test count:** 880 → ~892 (+12).

### 3.2 Tests

**$0 unit tests only.** No paid LLM run.

**12 new tests** in `tests/unit/evals/test_metrics.py` (or path implementer locates):

1. `test_citation_matches_exact_article` — `_citation_matches("44", "44") is True`.
2. `test_citation_matches_article_expected_apartado_emitted` — `_citation_matches("2.2", "2") is True` AND `_citation_matches("3.1", "3") is True`.
3. `test_citation_matches_exact_apartado` — `_citation_matches("6.1", "6.1") is True`.
4. `test_citation_matches_apartado_expected_different_apartado_emitted` — `_citation_matches("6.2", "6.1") is False`.
5. `test_citation_matches_apartado_expected_article_emitted` — `_citation_matches("6", "6.1") is False`.
6. `test_citation_matches_different_article_apartado_levels` — `_citation_matches("5.1", "6") is False` AND `_citation_matches("106.1", "6") is False` (no prefix-collision via startswith).
7. `test_citation_matches_different_article_both_levels` — `_citation_matches("5.1", "6.1") is False`.
8. `test_compute_citation_metrics_holdout_scenario` — `emitted=["2.2","3.1","3.2","3.3"], expected=["2","3"]` → precision=1.0, recall=1.0.
9. `test_compute_citation_metrics_h8_apartado_match` — `emitted=["6.1"], expected=["6.1","6.2"]` → precision=1.0, recall=0.5.
10. `test_compute_citation_metrics_h8_wrong_apartado_stays_miss` — `emitted=["6.3","6.7"], expected=["6.1","6.2"]` → precision=0.0, recall=0.0.
11. `test_compute_citation_metrics_chat_028_article_level_expected` — both `emitted=["44.1"]` and `emitted=["44"]` vs `expected=["44"]` → precision=1.0, recall=1.0.
12. `test_compute_citation_metrics_mixed_emitted_partial_coverage` — `emitted=["2.2","3.1","99.1"], expected=["2","3","5"]` → precision=2/3, recall=2/3.

**Pre-existing `compute_citation_metrics` tests:** implementer at T1 grep-checks `compute_citation_metrics` usage in `tests/`. Any tests pinning the OLD set-intersection semantics get adjusted 1-for-1 as part of T1 (instrument-change consequence, not scope creep — same pattern as v0.1.17.1 T2's `test_classify_other_when_non_refusal_prose` rename). Expected: minimal pre-existing tests.

### 3.3 Backend / production behavior

ZERO change. The eval-instrument fix lives in `evals/metrics.py` only. `src/regulaitor/citation/validator.py` (the §6-invariant guardian) stays byte-unchanged. Production-side citation VALIDATION (does the citation exist + match literally) is unaffected.

Backend H1-H5/H7 + Auditor + citation-validator + Pydantic schemas + DTOs + Analyst prompts v1.0-v1.4 + gold set ALL byte-unchanged. Verified by 5 git-diff HARD checks at T6 pre-closure gate.

---

## 4. Done-when (closure criteria)

All controller-verified at T6 (pre-closure gate) before squash-merge:

1. **Gate authoritative**: `uv run pytest -m "not slow"` → ≥892 passed / 0 failed / 1 skipped (carry) / ≥90% coverage exit 0.
2. **mypy strict**: `uv run mypy src` → Success 71 files exit 0 (UNCHANGED — v0.1.18 adds no `.py` under `src/`).
3. **Redteam smoke carry**: `make redteam-smoke` → block_rate 0.92 (= v0.1.14/v0.1.15/v0.1.16/v0.1.17/v0.1.17.1 carry). `git checkout -- redteam/reports/latest.md` after run.
4. **HARD invariant 1 — §6 byte-unchanged**: `git diff main...HEAD -- src/regulaitor/agents/auditor.py src/regulaitor/citation/validator.py` empty.
5. **HARD invariant 2 — entire `src/` byte-unchanged**: `git diff main...HEAD --stat -- src/` returns no output.
6. **HARD invariant 3 — eval pipeline non-metric byte-unchanged**: `git diff main...HEAD -- evals/judge.py evals/cache.py evals/harness.py evals/schemas.py evals/report.py` empty. ONLY `evals/metrics.py` changes under `evals/`.
7. **HARD invariant 4 — Analyst prompts v1.0-v1.4 byte-unchanged**: `git diff main...HEAD -- src/regulaitor/agents/prompts/analyst/system.v1.{0,1,2,3,4}.md` empty.
8. **HARD invariant 5 — gold set byte-unchanged**: `git diff main...HEAD -- evals/gold_set.jsonl` empty.
9. **Re-rendered holdout validation**: `evals/reports/h15/holdout-v1.2-chat.md` citation_recall for the chat cases jumps from 0.00 to a measurable value > 0. §22.22 honest framing: confirms the instrument-artifact-not-quality narrative was correct; exact value is whatever the recomputation produces, not promised.
10. **Production behavior unchanged**: `analyst.py` env seam from ADR-0016 untouched; production default v1.0 still byte-identical; v1.4 opt-in env still works. (Implicit from HARD invariant 2.)

---

## 5. Out of scope (explicit non-goals)

- **Paid LLM run** — $0 throughout. All re-rendering uses existing 381-entry judge cache.
- **Gold set re-annotation** — gold byte-unchanged per D2; chat-028 article-level outlier is intentional.
- **Backend touch** — entire `src/` byte-unchanged.
- **Analyst prompt change** — v1.0-v1.4 byte-unchanged; production default routing unchanged.
- **Citation/validator change** — `src/regulaitor/citation/validator.py` byte-unchanged. Production-side §6 validation is NOT v0.1.18's scope.
- **New gold cases** — regression-zero on gold corpus.
- **Partial-credit aggregation semantics** — rejected in D1 (Option C alternative).
- **v1.4 production-default flip** — deferred to v0.1.20 acceptance ritual per ADR-0021/ADR-0023.
- **v0.1.20 paid-run authorization** — explicitly waits for user budget recharge.
- **`Citation` Pydantic schema change** — schema byte-unchanged.
- **`_format_articulo` change** — still produces `"X.Y"` or `"X"`; the new match rule operates on its output.
- **CI gating on re-rendered historical deltas** — re-rendered reports are committed as evidence, not as automated gate inputs.
- **Document-mode citation metrics fix** — doc-mode uses the same `compute_citation_metrics` function so the fix applies transitively; no separate doc-mode work needed.
- **Backfilling pre-H8 reports** — only the canonical 3 reports (latest.md + candidate-v1.2.md + holdout-v1.2-chat.md) + any in `h15-1/` / `h15-2/` get re-rendered. Pre-H10 historical artifacts (none expected) stay frozen.

---

## 6. Alternatives considered

Documented at length under D1, D2, D3 above. Summary:

- **Normalize both to article granularity** (D1 reject) — loses H8 apartado precision signal.
- **Hierarchical with partial credit** (D1 reject) — YAGNI; complicates aggregation.
- **Light gold audit (chat-028 + spot-checks)** (D2 reject) — no evidence of drift to motivate.
- **Full gold audit (all 64)** (D2 reject) — high-probability YAGNI.
- **Freeze historical reports + caveat** (D3 reject) — permanent §22.22 caveat to carry through TFM memoria.

---

## 7. Consequences

**Positive:**

- **H15.1 §22.22 design-defect resolved**: the instrument-artifact-not-quality narrative is no longer needed; v1.2's actual citation quality becomes visible retroactively.
- **v0.1.20 paid bundle measurement gets a fair denominator**: no more granularity-mismatch confound on the 35-of-64 article-level expected cases.
- **§6 invariant strengthened (interpretive distinction)**: production-side citation VALIDATION (citation/validator.py) is byte-unchanged; the EVAL precision/recall metric fix is cleanly separated. TFM defense narrative gains clarity on the two layers.
- **Backward consistency**: retrospective re-rendering of canonical historical reports means v0.1.20 comparison numbers are apples-to-apples vs H10/H15/H15.1.
- **$0 milestone**: cache-mining re-render + unit tests; no paid LLM.
- **Hierarchical containment is reusable**: the helper `_citation_matches` is exportable and can be used by future eval extensions or third-party consumers of the precision/recall computation.

**Negative / accepted (per §22.22):**

- **The match rule's asymmetry adds cognitive load**: expected-side granularity controls the match leniency. Future eval contributors need to understand the asymmetry (documented in `_citation_matches` docstring + ADR-0024 + this spec).
- **No empirical validation of the v0.1.20 measurement narrative in v0.1.18**: v0.1.18 ships the prerequisite; v0.1.20 paid run validates that the v1.4 prompt + retrieval levers actually improve citation quality under the corrected instrument.
- **chat-028's article-level annotation is treated as intentional based on inspection**: if future annotators decide chat-028 should have been apartado-level, the v0.1.18 instrument silently still works (article-expected matches any apartado of art 44). The choice is reversible without instrument change.
- **`startswith` check on string prefix**: deliberately defended against by `_citation_matches("106.1", "6") is False` test (no prefix-collision: "106.1" does NOT start with "6." because "1" precedes the dot, but Python's `startswith("6.")` would only match if the string literally starts with "6.", so "106.1" correctly returns False). The test pins this anti-regression.
- **Re-rendered historical reports change committed values**: pre-v0.1.18 commit history's report files are different from v0.1.18+ committed report files. Git history makes this auditable; the v0.1.18 closure docs make the change explicit.
- **No partial-credit signal**: a case where the Analyst cites the right article but the wrong apartado (e.g. expected=["6.1"], emitted=["6.2"]) scores 0.0/0.0, same as completely-wrong-article. A future v0.1.19+ could add partial-credit if v0.1.20 measurement surfaces this as a meaningful distinction.

---

## 8. References

- Spec: this file.
- ADR-0010 (H8 evaluation harness — original metric instrument).
- ADR-0015 (H14 cross-corpus expansion — introduced article-level expected style).
- ADR-0017 (H15.1 retriever optimization — the §22.22 disclosure this resolves).
- ADR-0021 (v0.1.16 dual-layer thresholds — v0.1.20-bar measurement venue).
- ADR-0023 (v0.1.17.1 no-Answer fix — preceding eval-side milestone; pattern reference).
- Source data: `evals/reports/latest.md`, `evals/reports/h15/*.md`, `evals/gold_set.jsonl`.
- Existing instrument: `evals/metrics.py::_format_articulo`, `evals/metrics.py::compute_citation_metrics`.
- Future paid validation: v0.1.20 paid bundle (when budget recharges).
- Memory: `v0.1.17.1_closed_v0.1.18_starting.md` (carry-forward context).

---

## 9. Closure ceremony

Same pattern as v0.1.17.1 / v0.1.17 / v0.1.16:

- **Tag:** `v0.1.18-citation-granularity` (decimal sub-milestone naming).
- **Squash-merge** `feat/v0.1.18-citation-granularity` → main; annotated tag on squash commit.
- **Post-merge `<squash-sha>` populate** in 5 spots (targeted Edits; NEVER global find-and-replace):
  1. `docs/adr/0024-citation-granularity.md` Status field.
  2. `docs/technical_decisions_log.md` §v0.1.18 header line.
  3. `docs/evidence_matrix.md` v0.1.18 tag-table row.
  4. `CLAUDE.md` §16.3 H15.X v0.1.18 entry.
  5. `CLAUDE.md` §27 Hitos cerrados v0.1.18 bullet.

  The intentional `<squash-sha>` tokens in `docs/retriever_optimization.md:512` + `docs/superpowers/{plans,specs}/*.md` (15+ files including this spec) MUST NOT be touched.

- **Memory roll-forward**: rename `memory/v0.1.17.1_closed_v0.1.18_starting.md` → `memory/v0.1.18_closed_v0.1.19_starting.md`. Rewrite for v0.1.18 closed + v0.1.19 starting (Auditor RHR aggregation semantics + Council binding ON). Update `MEMORY.md` line 3 pointer.

- **Branch delete**: `git branch -D feat/v0.1.18-citation-granularity` (squash workflow).

- **Push only when user authorizes** separately.

Plan maximalist microhito **12/12 done** (final $0 microhito). Next: **v0.1.19** (Auditor RHR aggregation semantics + Council binding ON — the §6-invariant-adjacent work) · **v0.1.20** (single paid validation A/B when user recharges budget) · then return to **H16** (HF Spaces deploy) + **H17** (TFM cierre académico).
