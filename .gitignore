# User preference: NEVER track .env.example — single .env only (overrides CLAUDE.md §22.6)
.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
.eggs/
.Python
build/
dist/
sdist/
wheels/
*.whl

# Virtualenv
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
.coverage.*
htmlcov/
coverage.xml
.cache/
.hypothesis/

# Notebooks
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp
*~

# Env / secrets
.env
.env.local
.env.*.local

# Claude Code settings — both files are per-machine because the harness auto-saves
# permission grants to settings.json. If we ever want a team-policy file, we will
# create .claude/settings.team.json explicitly and force-add it.
.claude/settings.json
.claude/settings.local.json

# Superpowers brainstorming visual companion — local mockup cache
.superpowers/

# OS
.DS_Store
Thumbs.db

# Project: corpus and outputs (corpus/manifests/ IS tracked)
# corpus/raw and corpus/processed are tracked via Git-LFS (see .gitattributes)
# corpus/indexes is the LanceDB store, kept local per machine
corpus/indexes/
*.lance
evals/reports/*
!evals/reports/latest.md
# H12 A/B arm reports — tracked as TFM evidence (contaminated/partial; see
# docs/cost_analysis.md caveats). Mirrors the !latest.md exception.
!evals/reports/latest.evaluation.md
!evals/reports/latest.cost.md
!evals/reports/latest.council.md
# v0.1.20 paid validation A/B reports — tracked as TFM evidence (paid measurement
# artifacts; ARM A baseline + ARM B candidate + comparison + safety floor +
# probe summaries + SKIP/PROCEED decision). See spec docs/superpowers/specs/
# 2026-05-23-v0.1.20-paid-validation-design.md.
!evals/reports/v0.1.20/
!evals/reports/v0.1.20/*
# v0.1.21 quorum diagnostic + closure reports — tracked as TFM evidence
!evals/reports/v0.1.21/
!evals/reports/v0.1.21/*
# v0.1.22 paid validation reports — tracked as TFM evidence (cumulative-impact
# measurement vs v0.1.20 ARM B baseline; includes preserved probe-attempt-{1,2,3}
# evidence files for §22.22 disclosure of SSL + Capa A infrastructure block + fix).
# See ADR-0029 + spec docs/superpowers/specs/2026-05-24-v0.1.22-paid-validation-design.md.
!evals/reports/v0.1.22/
!evals/reports/v0.1.22/*
# v0.1.22.1 verdict-match drop diagnostic reports — tracked as TFM evidence
# ($0 cache-mining; 4-hypothesis attribution + v0.1.23 decision-tree). See
# spec docs/superpowers/specs/2026-05-25-v0.1.22.1-verdict-diagnostic-design.md.
!evals/reports/v0.1.22.1/
!evals/reports/v0.1.22.1/*
# v0.1.23 Auditor lenient quorum paid validation reports — tracked as TFM evidence
# (1-arm fresh post-fix vs cached v0.1.22-prod baseline; ~€2 paid; Design B per
# ADR-0030). See spec docs/superpowers/specs/2026-05-25-v0.1.23-auditor-lenient-quorum-design.md.
!evals/reports/v0.1.23/
!evals/reports/v0.1.23/*
# v0.1.24 gold alignment + AuditResult decomposition — tracked as TFM evidence
# ($0 mini-milestone; O1 acceptable_verdicts + O2 failed_check; re-aggregation
# diagnostic on cached v0.1.22/v0.1.23 data). See ADR-0031.
!evals/reports/v0.1.24/
!evals/reports/v0.1.24/*
# v0.1.24.1 per-Finding Auditor path diagnostic — tracked as TFM evidence
# ($0 mini-milestone; cross-version comparison v0.1.22 vs v0.1.23; NO new ADR).
!evals/reports/v0.1.24.1/
!evals/reports/v0.1.24.1/*
# v0.1.25 Auditor Strict-Answer partial-routing softening (Design H D2) paid
# validation reports — tracked as TFM evidence. See ADR-0032 + spec
# docs/superpowers/specs/2026-05-26-v0.1.25-auditor-partial-routing-design.md.
!evals/reports/v0.1.25/
!evals/reports/v0.1.25/*
# v0.1.27 doc-mode paid validation reports — tracked as TFM evidence
# (cost_per_doc_eur measurement + segmenter v0.1.14 efficacy at scale; closes
# H15.2 carry-forward "doc-mode never measured post-segmenter fix"). No new ADR.
!evals/reports/v0.1.27/
!evals/reports/v0.1.27/*
# v0.1.29 chat-016 D Mirror paid validation reports — tracked as TFM evidence
# (all-blocked routing softening; ADR-0034; +0.08 verdict_match on H10 25-case;
# chat-016 BLOCK → PASS as predicted + 2 bonus flips).
!evals/reports/v0.1.29/
!evals/reports/v0.1.29/*
evals/cache/
evals/reports/archive/
# v0.1.8 — per-case JSONL checkpoints (runtime artifacts; correlated to reports
# by run_id but not tracked).
evals/checkpoints/
redteam/reports/*
!redteam/reports/latest.md
notebooks/outputs/
.claude/scheduled_tasks.lock

# Spurious file created by Windows bash touch emulation — never commit
null

# User-local working files / external tooling — NOT part of the project.
# Accidentally swept into commit 682f3bb by a too-broad `git add`; untracked
# in H11 (git rm --cached, kept on disk). n8nac (n8n-architect) auto-generated
# tooling + user scratch. See decisions log §H11 / evidence_matrix.
AGENTS.md
.agents/
.github/agents/
.n8nac/
# Vercel/npx-installed external skills (v0.1.27+): managed per-machine via
# `npx skills add ...`; reproducible from skills-lock.json (committed).
# Project-own skills under .claude/skills/{citation-validator,...} stay tracked.
.claude/skills/web-design-guidelines/
evals/_skeleton.jsonl
